# 프로젝트 아키텍처 분석 보고서: fluency-loop (llm-pg)

## 1. 프로젝트 개요
**fluency-loop (llm-pg)**는 AI 기술을 활용하여 실시간으로 사용자의 언어 학습을 지원하고 즉각적인 피드백을 제공하는 시스템입니다. 본 프로젝트는 다음과 같은 핵심 가치를 지향합니다:

*   **고성능 및 안정성**: Rust와 Tokio 런타임을 기반으로 한 실시간 데이터 처리.
*   **확장성**: Provider 패턴을 도입하여 다양한 외부 API 및 하드웨어와의 유연한 통합.
*   **복잡한 상태 관리**: EventHub를 통한 이벤트 기반 상태 추적 및 시스템 정합성 유지.

---

## 2. 핵심 아키텍처 상세 분석

### [crates/core] - 도메인 모델 및 역할 분리
`core` 모듈은 시스템의 핵심 비즈니스 로직과 데이터 구조를 정의하며, 실제 구현부와 도메인 로직을 엄격히 분리합니다.
*   **구조**: `coach`, `learner`, `conversation_partner` 등 각 참여자의 역할을 독립적인 모듈로 구성.
*   **특징**: `domain.rs`에서 정의된 `SessionId`, `SegmentId`, `TranscriptCommit` 등의 데이터 구조를 기반으로 작동하며, 각 역할은 상호 독립적인 로직을 수행합니다.
*   **장점**: 비즈니스 규칙의 변화가 시스템 구현체에 직접적인 영향을 주지 않으므로 유지보수성이 매우 높습니다.

### [crates/pipeline] - 비동기 처리 엔진
`pipeline` 모듈은 실시간 오디오 데이터 처리를 위한 핵심 파이프라인을 담당합니다.
*   **기술적 기반**: `Tokio` 기반의 멀티태스킹과 `mpsc`, `broadcast` 채널을 통한 효율적인 통신 수행.
*   **프로세스 워크플로우**:
    1.  **Recorder**: 실시간 오디오 데이터 수집.
    2.  **Segmenter**: 음성을 분석 가능한 단위로 분할.
    3.  **STT (Speech-to-Text)**: 텍스트 변환 및 신뢰도(Confidence) 관리.
    4.  **Stabilizer**: 노이즈 제거 및 문맥적 정교화 처리.
*   **EventHub & Projection**: 복잡한 상태 변화를 이벤트 기반으로 추적하며, 'Projection' 구조를 통해 UI나 타 모듈에 안정적으로 가공된 상태 데이터를 공급합니다.

### [crates/providers] - 하드웨어/소프트웨어 추상화
시스템의 외부 의존성을 최소화하고 유연성을 확보하기 위해 **Provider 패턴**을 적용합니다.
*   **구조**: `mock`, `real`, `stubs`를 구분하여 구현체 분리.
*   **특징**: 실제 마이크 장치나 Google STT와 같은 특정 서비스에 대한 직접적인 의존성을 제거하고 인터페이스로 추상화합니다.
*   **효과**: 하드웨어 종속성 없는 개발 환경 구축 및 단위 테스트의 용이성을 극대화합니다.

### [crates/ui-tui] - 인터랙션 계층
사용자와 시스템 간의 상호작용을 담당하는 레이어입니다.
*   **역할**: 터미널 기반 UI(TUI)를 제공하여 사용자 경험을 지원.
*   **특징**: 핵심 로직(`core`, `pipeline`)과 렌더링 로직이 완전히 분리되어 있습니다. 이는 향후 GUI나 Web 프론트엔드로 전환 시, 비즈니스 로직의 수정 없이 UI 모듈만 교체하면 되는 높은 확장성을 제공합니다.

---

## 3. 기술적 강점 요약
1.  **고성능/안전성**: Rust와 Tokio 조합을 통해 실시간 오디오 처리 환경에서 발생할 수 있는 동시성 문제를 해결하고 고성능 신뢰성을 확보했습니다.
2.  **확장형 구조**: Provider 패턴의 도입으로 새로운 AI 모델이나 다양한 하드웨어 환경에 즉각적으로 대응할 수 있습니다.
3.  **상태 일관성**: EventHub 기반의 상태 관리 시스템은 복잡한 비동기 환경에서도 데이터 정합성을 유지하는 핵심 기둥 역할을 합니다.

---

## 4. 최종 결론
`fluency-loop (llm-pg)` 프로젝트는 고도로 모듈화된 아키텍처를 기반으로 설계되었습니다. 도메인 분리, 비동기 파이프라인, 추상화된 프로바이더 구조의 조화는 향후 다국어 지원 확장이나 복잡한 피드백 알고리즘의 고도화 등 시스템 규모 확장에 매우 유리한 기반을 제공합니다.
---