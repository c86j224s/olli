# 프로젝트 아키텍처 분석 보고서

## 1. 프로젝트 개요
- **프로젝트명**: fluency-loop (llm-pg)
- **특징**: Rust/Tokio 기반의 고성능 실시간 언어 학습 및 피드백 시스템.
- **핵심 가치**: 확장 가능한 Provider 패턴, 안정적인 EventHub 기반 상태 관리, 모듈화된 파이프라인.

## 2. 상세 아키텍처 분석

### [crates/core] (도메인)
- **역할 분리**: 'Coach', 'Learner', 'ConversationPartner' 등 역할별로 독립된 로직을 보유합니다.
- **비즈니스 로직 보호**: `domain.rs`를 기반으로 설계되어 핵심 비즈니스 규칙과 기술적 구현(Infrastructure) 간의 결합도를 낮췄습니다.

### [crates/pipeline] (처리 엔진)
- **고성능 비동기 처리**: `Tokio` 프레임워크를 활용하여 실시간 데이터 처리를 위한 비동기 멀티태스킹을 구현했습니다.
- **데이터 흐름(Pipeline)**: 
  - `Recorder(입력)` $\rightarrow$ `Segmenter(분할)` $\rightarrow$ `STT(인식)` $\rightarrow$ `Stabilizer(정제)` 단계로 이어지는 파이프라인 구조를 가집니다.
- **상태 관리**: `EventHub` 및 `Projection` 기술을 통해 복잡한 상태 변화를 일관되게 관리합니다.

### [crates/providers] (추상화)
- **의존성 제거**: Provider 패턴을 도입하여 하드웨어나 외부 API에 대한 직접적인 의존성을 제거했습니다.
- **유연한 확장성**: 'mock', 'real', 'stubs' 구분을 통해 개발/테스트 환경에서의 편의성을 높이고, 새로운 서비스 통합 시 신속하게 대응할 수 있는 구조를 확보했습니다.

### [crates/ui-tui] (인터랙션)
- **관심사 분리(SoC)**: 인터페이스와 비즈니스 로직을 완전히 분리하였습니다.
- **플랫폼 확장성**: 이러한 설계를 통해 추후 Web, Mobile 등 다양한 사용자 접점(UI 플랫폼)으로의 확장이 용이합니다.

## 3. 기술적 강점
- **Rust & Tokio 활용**: 고성능 및 저지연 실시간 데이터 처리를 보장합니다.
- **높은 유지보수성**: 철저한 관심사 분리(SoC)를 통해 코드 재사용성을 극대화하고 시스템 복잡도를 관리합니다.
- **신속한 프로토타이핑**: 추상화된 레이어를 기반으로 새로운 기능이나 프로토콜을 빠르게 통합할 수 있는 구조를 갖추고 있습니다.

## 4. 결론
본 시스템은 매우 견고하고 정교하게 설계되었습니다. 현재 요구사항을 완벽히 충족할 뿐만 아니라, 향후 다국어 지원 및 다양한 기능 확장을 수용하기에 최적의 기반 구조(Foundation)를 갖추고 있습니다.